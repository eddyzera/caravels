import { Container } from "../components/container/styled"
import { Header } from "../components/header/style"

export const Layout = () => {
  return (
    <Header>
      <Container>
        hello header
      </Container>
    </Header>
  )
}