import * as NextLink from 'next/link'
import { ReactNode } from 'react'

interface LinkProps {
  children: ReactNode
  anchor: string
}

export const Link = ({ children, anchor }: LinkProps) => {
  
}